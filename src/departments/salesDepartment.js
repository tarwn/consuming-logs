module.exports = class SalesDepartment {
    constructor(centralDatabase) {
        this._centralDatabase = centralDatabase;
    }

    // generateNewOrders() {
    //     // start super simple - if we have available
    //     // capacity, this sales team can find orders to
    //     // fill it perfectly

    //     const availableCapacity = this._centralDatabase.getAvailableProductionScheduleCapacity();
    // }
};
